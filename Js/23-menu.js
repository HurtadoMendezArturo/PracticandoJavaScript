let menu = prompt ("Ingrese una de las siguientes opciones\nARCHIVO\nBUSCAR\nSALIR");

if (menu == "archivo" || menu == "ARCHIVO") {
    document.write(`Escogiste la opción ${menu}`);

}else if (menu == "buscar" || menu == "BUSCAR") {
    document.write(`Escogiste la opción ${menu}`);

}else if (menu == "salir" || menu == "SALIR") {
    document.write(`Escogiste la opción ${menu}`);

} else {
    document.write("Escogiste una opción erronea");
} 